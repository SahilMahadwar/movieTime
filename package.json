{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "scripts": {
    "start:web": "yarn workspace web dev",
    "start:api": "yarn workspace api start:dev"
  },
  "packageManager": "yarn@3.2.1",
  "dependencies": {
    "dotenv": "^16.0.2"
  }
}
